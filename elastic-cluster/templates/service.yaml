apiVersion: v1
kind: Service
metadata:
  name: {{ include "elastic-cluster.fullname" . }}
  labels:
    {{- include "elastic-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elastic-cluster.selectorLabels" . | nindent 4 }}
